kw1: CREATE USER 'Agnieszka'@'localhost' IDENTIFIED BY 'haslo4';
kw2: SELECT AVG( rocznik ) FROM samochody WHERE marka = 'opel';
kw3: SELECT s.marka, k.imie FROM samochody s INNER JOIN wypozyczenia w ON s.id = w.samochody_id INNER JOIN klienci k ON w.klienci_id = k.id;
kw4: ALTER TABLE klasa DROP COLUMN cena;